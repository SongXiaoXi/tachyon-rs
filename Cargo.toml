[package]
name = "hpc"
version = "1.0.0"
edition = "2021"

[profile.release]
lto = "fat"
panic = "abort"
opt-level = 3
codegen-units = 1

[profile.dev.package."*"]
opt-level = 3

[lib]
crate-type = ["cdylib"]
bench = false

[dependencies]
cfg-if = "1.0.0"